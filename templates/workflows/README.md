# LoopPlane Workflow Templates

Workflow templates generate reproducible workflow drafts from small JSON
presets. They are deterministic data files, not plugins, and they do not execute
code.

Use `loopplane template list` to inspect available templates and
`loopplane workflow create --preset <file>` to create a workflow from a preset.

